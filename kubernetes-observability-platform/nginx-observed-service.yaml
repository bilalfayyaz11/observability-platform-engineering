apiVersion: v1
kind: Service
metadata:
  labels:
    app: nginx-observed
  name: nginx-observed
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: nginx-observed
  type: ClusterIP
status:
  loadBalancer: {}
