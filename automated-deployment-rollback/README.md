
The final delimiter must be written alone:

    EOF

Do not paste README paragraphs directly into the shell without the opening heredoc command.

## Future Improvements

- Add signed and verified Git tags
- Add deployment approval gates
- Add automated smoke tests
- Add configurable health endpoints
- Add retry and timeout policies
- Add structured JSON audit logs
- Add centralized log forwarding
- Add backup retention management
- Add release artifact checksums
- Add rollback selection by version
- Add database migration recovery
- Add pre-deployment configuration validation
- Add automatic rollback based on error rates
- Add Prometheus deployment metrics
- Add Grafana release dashboards
- Add failure notifications
- Add CI/CD pipeline integration
- Add immutable release artifacts
- Add container-based deployment support
- Add Kubernetes rollout integration
